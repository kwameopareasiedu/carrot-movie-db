import { MantineThemeOverride } from "@mantine/core";

// Theme configuration for Mantine UI library
const defaultTheme: MantineThemeOverride = {
  fontFamily: "Poppins, sans-serif",
  primaryColor: "orange"
};

export default defaultTheme;
